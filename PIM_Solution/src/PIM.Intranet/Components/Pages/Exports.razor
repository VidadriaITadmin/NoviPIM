@page "/izvozi"
@attribute [Authorize]
@inject NavigationManager Navigation

@* Ta stran je bila dvojnik strani „Izhod na splet" — ista pokritost stolpcev, isti razlogi za
   neobjavo, isti profili. Uporabnik jo je 2026-08-28 odpisal, zato je tu samo se preusmeritev:
   stari zaznamki in povezave koncajo tam, kjer je vsebina zdaj. Podstrani /izvozi/* (mnozicno,
   obvestila, profili) ostajajo nedotaknjene. *@

<PageTitle>Izhod na splet</PageTitle>

@code {
  protected override void OnInitialized() => Navigation.NavigateTo("splet", replace: true);
}
