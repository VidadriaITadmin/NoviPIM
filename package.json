{
  "name": "NoviPIM",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "echo \"(lint se doda kasneje)\""
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
